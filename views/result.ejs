<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Image Recognition Result</title>
    <link rel="stylesheet" href="/output.css">
    <script src="/reload.js"></script>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center p-6">
    <h1 class="text-4xl font-bold text-gray-800 mb-6">Image Recognition Result</h1>

    <img src="<%= imageUrl %>" alt="Uploaded Image" class="max-w-sm rounded-lg shadow-lg mb-6">

    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Predictions:</h2>
    <ul class="space-y-3 w-full max-w-md">
        <% predictions.forEach(pred=> { %>
            <li class="bg-white shadow-md rounded-lg p-4 flex justify-between items-center">
                <span class="font-medium text-gray-800">
                    <%= pred.label %>
                </span>
                <span class="text-gray-600">
                    <%= (pred.score * 100).toFixed(2) %>%
                </span>
            </li>
            <% }) %>
    </ul>

    <a href="/"
        class="mt-8 px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg shadow hover:bg-blue-700 transition">Upload
        another image</a>
</body>

</html>